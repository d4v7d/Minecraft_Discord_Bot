# Dependencias (se reinstalan con "npm install", no hace falta subirlas)
node_modules/

# Tus tokens y datos reales — NUNCA subir esto a GitHub
.env

# Logs y basura del sistema/editor
*.log
.DS_Store
Thumbs.db
